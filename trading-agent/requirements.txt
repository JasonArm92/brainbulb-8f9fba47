# Core has no third-party dependencies. Optional:
# anthropic        # Brain (Claude Opus 5.5) for escalations and nightly rewrites
# typesafe-sdk>=0.7,<0.8  # Jev reflex engine; adapter checked against 0.7.1 + docs.typesafe.ai (2026-09-24)
